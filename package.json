{
    "name": "markdown",
    "version": "0.5.1",
    "description": "md — render markdown (and tex) in your terminal or browser with syntax highlighting, math, mermaid, and UI modes.",
    "type": "module",
    "license": "MIT",
    "bin": {
        "markdown": "./src/cli.ts",
        "md": "./src/cli.ts"
    },
    "files": [
        "src"
    ],
    "scripts": {
        "build": "bun build ./src/cli.ts --target=bun --outdir=dist",
        "start": "bun ./src/cli.ts"
    },
    "dependencies": {
        "@earendil-works/pi-tui": "^0.79.4",
        "chalk": "^5.5.0",
        "highlight.js": "^11.11.1",
        "marked": "^14.1.4",
        "shiki": "^4.2.0"
    },
    "devDependencies": {
        "@types/bun": "latest"
    }
}
